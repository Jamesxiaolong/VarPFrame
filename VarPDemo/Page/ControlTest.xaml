<UserControl x:Class="VarPDemo.Page.ControlTest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:CControl="clr-namespace:VarPControl.Controls;assembly=VarPControl"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1050">
    <Grid>
        <!--控件测试-->
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Grid Width="10" />
            <CControl:PButton PIcon="&#xe64f;" PIconVisibility="True" CornerRadius="16,0,0,16" Content="信息" AllowsAnimation="True"/>
            <CControl:PButton PIcon="&#xe628;" PIconVisibility="True" Content="疑问" AllowsAnimation="True"/>
            <CControl:PButton PIcon="&#xe628;" PIconVisibility="True" Content="警告" AllowsAnimation="True"/>
            <CControl:PButton PIcon="&#xe629;" PIconVisibility="True" CornerRadius="0,16,16,0" Content="错误" AllowsAnimation="True"/>
            <Grid Width="20" />
            <Grid Width="10" />
        </StackPanel>


    </Grid>
</UserControl>
